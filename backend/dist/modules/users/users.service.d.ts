import { Repository } from 'typeorm';
import { User } from '../../database/entities/user.entity';
import { Department } from '../../database/entities/department.entity';
import { SubDepartment } from '../../database/entities/sub-department.entity';
import { Position } from '../../database/entities/position.entity';
import { RegisterDto } from '../auth/dto/register.dto';
import { QueryUserDto } from './dto/query-user.dto';
import { UpdateProfileDto } from './dto/update-profile.dto';
import { BulkImportUsersDto } from './dto/bulk-import-user.dto';
import { ApiResponse } from '../../common/response';
export declare class UsersService {
    private readonly userRepo;
    private readonly departmentRepo;
    private readonly subDepartmentRepo;
    private readonly positionRepo;
    private readonly avatarDir;
    constructor(userRepo: Repository<User>, departmentRepo: Repository<Department>, subDepartmentRepo: Repository<SubDepartment>, positionRepo: Repository<Position>);
    uploadAvatar(userId: string, file: Express.Multer.File): Promise<ApiResponse>;
    deleteAvatar(userId: string): Promise<ApiResponse>;
    getAvatarFilePath(filename: string): string | null;
    private safeDeleteOldAvatar;
    create(dto: RegisterDto): Promise<ApiResponse>;
    bulkImport(dto: BulkImportUsersDto): Promise<ApiResponse>;
    getMyTeam(currentUser: any, query?: QueryUserDto): Promise<ApiResponse>;
    findAll(query?: QueryUserDto): Promise<ApiResponse>;
    findOne(id: string): Promise<ApiResponse>;
    update(id: string, dto: Partial<RegisterDto>): Promise<ApiResponse>;
    updateProfile(userId: string, dto: UpdateProfileDto): Promise<ApiResponse>;
    remove(id: string): Promise<ApiResponse>;
    toggleActive(id: string): Promise<ApiResponse>;
}
