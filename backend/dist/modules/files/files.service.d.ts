import { Repository } from 'typeorm';
import { SeminarFile } from '../../database/entities/seminar-file.entity';
import { Seminar } from '../../database/entities/seminar.entity';
import { NotificationsService } from '../notifications/notifications.service';
import { ModelConverterService, ModelMetadata } from './model-converter.service';
import { MarkCleanupDto } from './dto/mark-cleanup.dto';
import { ApiResponse } from '../../common/response';
import { Role } from '../../common/enums/role.enum';
export declare class FilesService {
    private readonly fileRepo;
    private readonly seminarRepo;
    private readonly notificationsService;
    private readonly modelConverter?;
    private readonly uploadBase;
    constructor(fileRepo: Repository<SeminarFile>, seminarRepo: Repository<Seminar>, notificationsService: NotificationsService, modelConverter?: ModelConverterService | undefined);
    private ensureDirectories;
    uploadSeminarFile(seminarId: string, file: Express.Multer.File, userId: string, userRole: Role): Promise<ApiResponse>;
    uploadMultipleSeminarFiles(seminarId: string, files: Express.Multer.File[], userId: string, userRole: Role): Promise<ApiResponse>;
    findBySeminar(seminarId: string): Promise<ApiResponse>;
    findOne(id: string): Promise<SeminarFile | null>;
    checkFilePermission(file: SeminarFile, currentUser: any, action: 'view' | 'download'): boolean;
    remove(id: string, userId: string, userRole: Role): Promise<ApiResponse>;
    markFilesForCleanup(dto: MarkCleanupDto, adminId: string): Promise<ApiResponse>;
    cancelCleanup(fileIds: string[]): Promise<ApiResponse>;
    forceDelete(fileId: string): Promise<ApiResponse>;
    getPendingCleanupFiles(): Promise<ApiResponse>;
    getAdminCleanupCandidates(): Promise<ApiResponse>;
    inspect3DModel(fileId: string): Promise<ApiResponse<ModelMetadata | null>>;
    private validateFileSizeAndType;
    private safeUnlink;
}
