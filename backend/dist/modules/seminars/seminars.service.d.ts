import { Repository } from 'typeorm';
import { Seminar } from '../../database/entities/seminar.entity';
import { SeminarFile } from '../../database/entities/seminar-file.entity';
import { Tag } from '../../database/entities/tag.entity';
import { Like } from '../../database/entities/like.entity';
import { Comment } from '../../database/entities/comment.entity';
import { SavedSeminar } from '../../database/entities/saved-seminar.entity';
import { CreateSeminarDto } from './dto/create-seminar.dto';
import { UpdateSeminarDto } from './dto/update-seminar.dto';
import { QuerySeminarDto } from './dto/query-seminar.dto';
import { ApiResponse } from '../../common/response';
import { Role } from '../../common/enums/role.enum';
import { SeminarStatus } from '../../common/enums';
export declare class SeminarsService {
    private readonly seminarRepo;
    private readonly fileRepo;
    private readonly tagRepo;
    private readonly likeRepo;
    private readonly commentRepo;
    private readonly savedRepo;
    constructor(seminarRepo: Repository<Seminar>, fileRepo: Repository<SeminarFile>, tagRepo: Repository<Tag>, likeRepo: Repository<Like>, commentRepo: Repository<Comment>, savedRepo: Repository<SavedSeminar>);
    create(userId: string, dto: CreateSeminarDto, userRole?: Role): Promise<ApiResponse>;
    toggleBookmark(seminarId: string, userId: string): Promise<ApiResponse>;
    getBookmarkedSeminars(userId: string, page?: number, limit?: number): Promise<ApiResponse>;
    findAll(query: QuerySeminarDto, currentUser?: any): Promise<ApiResponse>;
    findOne(id: string, currentUser?: any): Promise<ApiResponse>;
    update(id: string, userId: string, userRole: Role, dto: UpdateSeminarDto): Promise<ApiResponse>;
    updateStatus(id: string, status: SeminarStatus, userId: string, userRole: Role): Promise<ApiResponse>;
    reorderFiles(seminarId: string, fileIds: string[], userId: string, userRole: Role): Promise<ApiResponse>;
    getTargetSeminars(userId: string, page?: number, limit?: number): Promise<ApiResponse>;
    getPopularTags(): Promise<ApiResponse>;
    remove(id: string, userId: string, userRole: Role): Promise<ApiResponse>;
    getDashboardStats(currentUser?: any): Promise<ApiResponse>;
    private resolveTags;
    private normalizeTagName;
}
